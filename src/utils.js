export function isSupportCanvas(canvas) {
	return canvas && canvas.getContext;
}

/**
 * detect element is in boundary
 * return:
 *  out of boundary
 *  cross boundary
 *  in boundary
 */
function isBoundary(){

}
